_schema-version: "3.2"
ID: demo_cicd
version: 0.0.1
modules:
- name: demo_cicd-app-content
  type: com.sap.application.content
  path: .
  requires:
  - name: demo_cicd-repo-host
    parameters:
      content-target: true
  build-parameters:
    build-result: resources
    requires:
    - artifacts:
      - fioridemocicd.zip
      name: fioridemocicd
      target-path: resources/
- name: fioridemocicd
  type: html5
  path: fiori_demo_cicd
  build-parameters:
    build-result: dist
    builder: custom
    commands:
    - npm install
    - npm run build:cf
    supported-platforms: []
resources:
- name: demo_cicd-repo-host
  type: org.cloudfoundry.managed-service
  parameters:
    service: html5-apps-repo
    service-name: demo_cicd-html5-srv
    service-plan: app-host
- name: demo_cicd-destination-service
  type: org.cloudfoundry.managed-service
  parameters:
    config:
      HTML5Runtime_enabled: false
      init_data:
        instance:
          destinations:
          - Authentication: NoAuthentication
            Name: ui5
            ProxyType: Internet
            Type: HTTP
            URL: https://ui5.sap.com
          existing_destinations_policy: update
      version: 1.0.0
    service: destination
    service-name: demo_cicd-destination-service
    service-plan: lite
parameters:
  deploy_mode: html5-repo
